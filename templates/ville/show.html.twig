{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
    <h1>Ville</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ ville.nom }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ ville.codePostal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ville_index') }}" class="btn btn-light">Retour à la liste</a>
    <br>
    <a href="{{ path('app_ville_edit', {'id': ville.id}) }}" class="btn btn-warning">Modifier</a>

    {{ include('ville/_delete_form.html.twig') }}
{% endblock %}
